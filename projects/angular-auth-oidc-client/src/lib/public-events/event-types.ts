export enum EventTypes {
    /**
     *  This only works in the AppModule Constructor
     */
    ConfigLoaded,
    ModuleSetup,
    CheckSessionChanged,
    UserDataChanged,
    NewAuthorizationResult,
}
